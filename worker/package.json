{
  "name": "tao-ai-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/bundle-worker.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test ../tests/tao-ai-worker.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
